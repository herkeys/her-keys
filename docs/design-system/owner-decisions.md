# Owner decisions — visual checkpoint approval (2026-09-20)

Recorded verbatim effect; these decisions bind the rest of HK-FE-UI-01.

1. **Clay**: keep `#9C4A2F`. Do not deepen or lighten it.
2. **Plum**: stays reserved. It may support selective Her Keys
   intelligence/AI semantic treatments where the design system calls for it,
   but it must NOT become recurring Her Keys AI chrome or a separate section
   identity. Her Keys AI must still look like the same product.
3. **Selected chips**: keep solid clay fill with ivory text. This may remain
   one of the stronger accent moments (it communicates a real selected state).
   Do not expand this treatment indiscriminately to ordinary informational
   surfaces.
4. **Capacity / load segments**: use a more neutral ink/umber treatment rather
   than clay as the dominant capacity fill. Capacity is informational state,
   not primarily an action. Clay stays the application's decisive
   interaction/accent color. — Applied at K4 (LoadMeter) and K6 (onboarding
   progress re-evaluated: step progress is path navigation, kept accent).

## Routing-authority clarification (for the final report)

The additive `gallery: 'internal'` registration in
`src/domain/routeAccess.ts` is described as: an intentional shared
routing-authority modification, limited to the dev-only design gallery,
preserving the existing single routing-guard authority, producing no
domain/schema/migration/fingerprint semantic drift. It is not a foundation
touch in the backend sense — the guard table is the front-end routing
authority — but it is honestly a shared front-end file modification and is
reported as such.

Everything else at the visual checkpoint was approved.

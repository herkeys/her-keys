# HK-FEATURE-05 — Missing primitives register

Every capability Kids OS wanted that the repaired baseline (`14bd58e`) does not provide. **Nothing here was closed quietly inside
Feature 05.** Rows marked *owner checkpoint* are proposals only; nothing durable was built for them.

| Field key | Meaning |
|---|---|
| LOCAL? | Is a feature-local solution possible without a new shared durable concept? |
| DURABLE? | Would a real fix be a new durable semantic (entity, column, relationship, sync kind, RLS or grant)? |
| CHECKPOINT? | Is an owner checkpoint required before it is built? |

| ID | CAPABILITY | ACTUAL BASELINE STATE | WHY EXISTING SEMANTICS ARE INSUFFICIENT | IMPACT | LOCAL? | DURABLE? | CHECKPOINT? | DEFERRED SYSTEM | INTEGRATION TIMING |
|---|---|---|---|---|---|---|---|---|---|
| **MP-K-01** | Create a child | No mutation creates a `Child`. Empty households have 0 children; the demo seeds 2. Cloud child rows are server-created by the claim only; `member` is a mapping-only sync kind (D6) | A real household cannot use Kids at all without one | Kids would be a dead end for real users | **Pre-binding: YES** — `addChild` in a new domain file over the existing `Child` shape; claim v3 already carries every child. **Post-binding: NO** | Post-binding: **YES** (a server-owned creation path) | **YES for post-binding** → `HK_FEATURE_05_OWNER_CHECKPOINT_01.md`. Pre-binding: no (existing entity, existing claim) | Foundation / account | Owner decision, then a foundation build |
| **MP-K-02** | A *distinct backup* person for a commitment ("if the normal holder cannot, who?") | No fallback, backup or pickup concept. `Responsibility` allows exactly one live holder per thing (`state.ts:823-828`); `alternative_to` means "a lighter way to satisfy" (`structure.ts:20`) | Recording a second party needs a relation that says *fallback for*; overloading `alternative_to`/`requires`/`part_of` would mislead Today and Calendar (a `requires` edge would make an event read "blocked") | Kids can show whether the *arrangement* (the one holder) holds; it cannot show a second, independent option | **Partly** — arrangement readiness from responsibility truth, plus a `part_of` step for the work of arranging | Yes, if a distinct backup party is wanted | **NO for v1** (labels are defined over existing facts and worded so); **YES** if the owner wants a recorded second party | People OS / Co-Parent (F07) | With People OS |
| **MP-K-03** | Pickup authorization / who a school or court recognises | None. `authorization.ts` is *automation* authority (what Her Keys may do), unrelated | Feature 05 must not create a Kids-owned authorization registry (owner decision) | The fallback surface says plainly that it is planning only | n/a | n/a | n/a — decided by the owner | Co-Parent / Life Admin | F07 / F12 |
| **MP-K-04** | Remove / archive a child | No path (B4-P0-066, deferred) | Removal semantics (what happens to a child's rows) were proposed "separately if such a path is built" | Kids offers none (Tier 3 BH: SAFE-UNAVAILABLE) | No | Yes | Yes, when wanted | Foundation | Owner decision |
| **MP-K-05** | Rename / correct a child's name or birth date | No path. The cloud `household_members` row is read-only to the client | A local edit would diverge from the server row and could be overwritten on pull | Kids offers no child edit | No | Yes (same server path as MP-K-01) | Bundled with MP-K-01 | Foundation | With MP-K-01 |
| **MP-K-06** | People management (add / edit / archive a person) | `addPerson` / `archivePerson` exist as domain functions; **no UI calls them** | A handoff needs someone to hand to. People OS owns the network | Kids adds one *minimal* inline "add someone" (name + relationship, no contact details) inside the handoff flow only; no list, edit or archive screen | Yes (existing mutation) | No | No | People OS | People OS supersedes the inline control |
| **MP-K-07** | Any UI that writes a responsibility | Domain lifecycle exists; **no screen** used it | The distinctions ASSIGNED ≠ ACKNOWLEDGED ≠ ACCEPTED ≠ COVERED are only visible if somebody can record each | Kids performs only the transitions in ledger §9 | Yes | No | No | People OS | People OS |
| **MP-K-08** | A Life extension / registration mechanism | Life is hardcoded routes (`life/_layout.tsx:30-37`, `lifeStatus.ts:28-34`); none exists | Owner rule 3: not required before Feature 05 | Kids lives behind the existing `life/kids` route; the Life hub is untouched | Yes | No | No | Integration | Wave integration |
| **MP-K-09** | `attentionFor` counts `acknowledged` as "handled elsewhere" for `risk` | `reasoning/attention.ts:60-62` | Looser than ACKNOWLEDGED ≠ ACCEPTED | Kids consumes the primitive unchanged so it cannot contradict Today; Kids' own fact rows (`Unaccepted`, `Waiting for an answer`) state the distinction | Yes | No | No (shared-file change not made) | Foundation | Owner may tighten later |
| **MP-K-10** | `needsMePersonally` is `null` for anything never delegated | `responsibility.ts:220-234` | "Does this still need me?" is genuinely unknown for an undelegated item | Kids says "Nobody is recorded as handling this" and never "needs you" without a canonical reason | Yes | No | No | — | — |
| **MP-K-11** | Event end / length provenance (OD-C) | An assumed end is durably identical to a stated one | Same class as HA-010, for events | Kids shows an event's recorded start and end and never says "you said" | Yes | Yes (a future field) | Yes, when wanted | Calendar / F02–F03 | With OD-C |
| **MP-K-12** | Meal / Category child subject (IR-D9) | Cloud has the column, local has none | — | Meals cannot be attributed to a child and are not shown | No | Yes | Yes | Meals (F08) | F08 |
| **MP-K-13** | Second household member sees who is handling a child's item | `people` and `responsibilities` are `scope = 'personal'` (owner-private, `migration:1735…3485`) | Widening is an owner decision (SD4) | A second member reads child tasks/events but not the responsibility beside them | n/a | Yes | Yes, when wanted | People OS / F07 | With People OS |
| **MP-K-14** | A UI to record a prerequisite (`requires`) | `addDependency` exists; no UI calls it | — | Kids **reads** prerequisites through `standingOf` / `readinessOf` and preserves them on edit; it does not add a chooser for `requires` | Yes (deliberately not built) | No | No | Systems / Today | Later |
| **MP-K-15** | Confirm the length of a task saved before HA-010 | Such tasks read "unrecorded"; no dedicated path | — | Kids' edit path is one: touching the length field makes it hers (`durationSourceForSave`) | Yes | No | No | — | — |
| **MP-K-16** | Whether a handoff still has a holder who is on her list | `needsMePersonally` (`responsibility.ts:220`) reads only the responsibility own fields, so an accepted, off-her-list handoff to a person since ARCHIVED still answers "does not need her" | The foundation never asks whether the holder is still an active person | Kids files such an item under "needs you", says the person is no longer on her list, and reports the foundation answer unchanged | Yes | No | No (a shared-file change was not made) | Foundation / People OS | People OS or a foundation repair |

# HK-FEATURE-12 — Missing primitives and integration candidates

Everything F12 needed and did not find in WAVE3_BASE (`363e473`), and every seam it deliberately left for Wave 3 integration. Nothing
here was invented inside F12 to paper over a gap. Kept current at each milestone.

Format (integration-candidate):

| ID | Item | Current Owner | Needed Capability | Depends On | Target Integration | Blocking? | Notes |
|---|---|---|---|---|---|---|---|
| MP-12-01 | Local-id existence probe on SCOPED tables (M0 NOTE 2) | Foundation (Build 4 `tasks`/`events`/... uniqueness `(household_id, local_id)`; `sync_push` probe under RLS) | Owner-keyed uniqueness for personal-scope rows on scoped tables, or a collision path that answers "not yours" and "absent" identically | Shared schema + `sync_push` | Wave 3 foundation repair | NO for F12 | Confirms only a GUESSED local id exists (no content, no relationship). F12 mitigates for the Tasks it creates: their local ids carry ~128 random bits (`src/features/lifeAdmin/draftId.ts`). Affects every personal Task the product already creates. |
| MP-12-02 | Known-id FK existence probe on foundation relationship tables (M0 NOTE 1) | Foundation (`dependencies`, `evidence_links`, `external_references` typed FKs to `tasks`/`events`/... are `(id, household_id)`, not owner-keyed) | Owner-matching guard on cross-owner typed references, refusing "not yours" exactly like "absent" | Shared schema | Wave 3 foundation repair | NO for F12 | Requires a server uuid the member never receives. F12's own link table closes it (`private.guard_life_record_task_link`, SECURITY INVOKER; proven in 79, mutant LA7b). The same guard shape could be applied to the foundation tables. |
| MP-12-03 | PENDING-INTEGRATION — PRIVATE RECORD SEARCH | none (no search primitive exists) | A local search primitive that proves owner-private isolation and masking of result rows | Search architecture | Wave 3 integration or later | NO | Addendum S: F12 search NOT BUILT. Safe fields when built: title, type name, issuer name — never reference, note or location. |
| MP-12-04 | PENDING-INTEGRATION — RECORD/EVENT RELATIONSHIP | F12 + Calendar | An owner-private typed link LifeRecord ↔ Event (e.g. a DMV appointment), analogous to `life_record_task_links` | Event privacy semantics | Wave 3 integration | NO | Record dates (expires/renew/review) never create Events; no shared Event schema was changed. |
| MP-12-05 | LifeRecord → Co-Parent sharing | F07 (co-parent semantics) | An explicit, consented sharing primitive for a record or its metadata | F07 + a cross-account sharing model (none exists: `coparent-shared` is owner-only) | PENDING WAVE 3/4 INTEGRATION | NO | No automatic cross-scope sharing; a child subject does not imply sharing authority. |
| MP-12-06 | Life Admin → Money | F09 (sibling) | Money-owned amounts/payment state for records such as a policy or a tax form | F09 | PENDING WAVE 3 INTEGRATION | NO | F12 owns no amount, payment state, income, reimbursement or total. No F09 code imported. |
| MP-12-07 | Life Admin → Work/Career | F10 (sibling) | Career-owned workflow for résumé / offer letter / certificate records | F10 | PENDING WAVE 3 INTEGRATION | NO | F12 may hold generic record metadata only; no CareerOpportunity. |
| MP-12-08 | Life Admin → Me/Rebuild | F11 (sibling) | none required; a personal administrative record stays F12 truth | F11 | Wave 3 integration review | NO | A record is never a RebuildFocus. |
| MP-12-09 | Issuer / contact identity | F13 People OS (future) | Canonical People identity for issuers, attorneys, schools | F13 | Future | NO | F12 uses free-text `issuerName` only; no contact CRM. |
| MP-12-10 | Document file storage (secure vault) | none | Owner-private file storage with sync/reference durability, revocation and safe URL behaviour | Storage architecture | Future | NO | V1 is metadata-first: no upload, bucket, signed URL, OCR or extraction. The detail says Her Keys keeps details, not the document. |
| MP-12-11 | External document reference / URL | Foundation `ExternalReference` has no URL | A safe, non-authenticating durable reference to where a document lives | MP-12-10 | Future | NO | `locationHint` is her own words (≤ 120), never parsed, fetched or logged. |
| MP-12-12 | Notification delivery for `reviewOn` / `renewBy` | none (no notification system exists) | A notification primitive with dismissal semantics ("reminder dismissed ≠ requirement completed") | Notification architecture | PENDING-INTEGRATION | NO | Addendum L: dates surface only on the Life Admin home and detail. |
| MP-12-13 | Clipboard copy of a revealed reference | none (no clipboard module in dependencies) | A platform clipboard primitive (with a clear-after policy) | Dependency decision | Future | NO | Copy is SAFE-UNAVAILABLE and said so on screen. |
| MP-12-14 | LifeRecord → System (repeatable procedure) | Systems (F04) | Typed link record ↔ System for procedures such as an annual registration checklist | Systems | PENDING-INTEGRATION | NO | Addendum X: F12 builds no procedure templates. |
| MP-12-15 | Talk It Out → LifeRecord routing | F02 | Interpreter routing of "my passport expires in March" to a LifeRecord, a Task, or both | F02 interpreter | PENDING WAVE 3 INTEGRATION | NO | Addendum Y: no second capture pipeline; the interpreter is untouched. |
| MP-12-16 | Home → Life Admin relationships (`HK-INT-HOME-LIFEADMIN-01`, F06) | F06 + F12 | Typed link from a Home item to a warranty/manual record | Home model | Wave 3 integration | NO | F12 is now the owner of the record half; no Home file was touched. |
| MP-12-17 | Child archive / rename (foundation MP-K-04 / B4-P0-066) | Foundation | Child rename and archive, with an archived-subject projection | Foundation | Wave 3 foundation | NO | F12 resolves the subject by id to the CURRENT name and says "Child not found on this device" when a child is missing; it cannot show "archived" because the foundation has no child archive. |
| MP-12-18 | PENDING F12 FOLLOW-UP — EXPLICIT RECORD SUPERSESSION (F12-STRETCH-01) | F12 | `newRecord.supersedesLifeRecordId`; superseded DERIVED; no self/cycle; at most one direct active successor; chains; archive keeps the chain; history editable | F12 core (done) | F12 follow-up before or during Wave 3 integration | NO (outside the CORE floor, addendum AA) | Not built: see the ledger's stretch section for the recorded design. |
| MP-12-19 | USER-INITIATED PERMANENT RECORD DELETION | Product decision | A hard-delete path (local + cloud + tombstone semantics) | Product decision + tombstone architecture | Future product decision | NO | Addendum Z: V1 exposes clear-and-archive only; account deletion cascades through the foundation (FKs ON DELETE CASCADE from households/profiles). |
| MP-12-20 | Linked-Task-overdue as a Life Admin review signal | F12 + Today | A decision whether an overdue linked Task should also appear in Life Admin's Needs Review | Product decision | Wave 3 integration | NO | DD-12-10: excluded to avoid a second attention system (the Task already reaches Today). |
| HK-INT-W3-MEALS-SCAN-01 | The Meals semantic-boundary scan (`scripts-dev/meals-boundary-scan.cjs`, tests/meals/boundary.test.mjs) is structurally incompatible with every Wave 3 feature | F08 / integration | Re-scope the scan to Meals' own lane (or a per-feature declared-changes registry) | Integration | Wave 3 integration | NO for F12 (fails at ENTRY already) | ENTRY: fails on check G because `feature/09-money-os` forks from the same base. EXIT: additionally lists F12's new migration, tables, schemas, roots, sync kinds and files — every one of them intentional and explained in this ledger. F12 did not edit another feature's certification gate. F09 recorded the same (`HK-INT-MONEY-BOUNDARY-01`). |
| HK-INT-F12-RUNMJS-01 | `supabase/tests/run.mjs` pins "exactly N migrations", the table count (36) and the ENV A/C/D sequences | Test infrastructure | A migration registry the harness reads, so parallel features do not each rewrite the same lines | Integration | Wave 3 integration | NO | F12 added its migration everywhere F08/F05 did (quality gate, ENV A, ENV C, ENV D populated upgrade, 00-interlock 36 tables, private stack). Every sibling that adds a migration conflicts on these lines. |
| HK-INT-F12-SYNCPUSH-01 | `public.sync_push` replaced by F12 | Foundation sync | Merge of every Wave 3 `sync_push` replacement into one body | Integration | Wave 3 integration | NO | F12's body = F05's + two table names in the owner-keyed list (line-diff verified). A sibling that also replaces it must be merged, not ordered. |
| HK-INT-F12-CHANGELOG-01 | `change_log_entity_table_check` re-declared with the two F12 tables | Foundation sync | One combined entity list at integration | Integration | Wave 3 integration | NO | Drop + re-add in the F12 migration; siblings adding tables conflict on the same constraint. |
| HK-INT-F12-SYNCKINDS-01 | Two new core sync kinds (`lifeRecord`, `lifeRecordLink`) | Foundation sync | Merge of sibling kinds; the inventory test (`tests/hk-ir01/changeBridge.test.mjs`, now 31 / 29) | Integration | Wave 3 integration | NO | Registered by hand like `needsMe`; every `Record<SyncEntityKind, …>` map extended. |
| HK-INT-F12-TRANSPORT-01 | `failureFrom` withholds refused-row values from sync evidence | Platform transport (shared) | Keep at integration | — | Wave 3 integration | NO | A generic privacy repair found in M5 (every kind benefits); exported for its unit test. |
| HK-INT-F12-LIFEHUB-01 | Life hub row "Life Admin / Documents" (`app/(app)/life/index.tsx`) | Life hub | Combined registration of Wave 3 Life destinations (cf. `HK-INT-WAVE2-LIFE-REGISTRATION`) | Integration | Wave 3 integration | NO | One hard-coded row, count only; `life/_layout.tsx` untouched (the route declares its own header). |
| HK-INT-F12-STATE-01 | Two new `AppState` roots (`lifeRecords`, `lifeRecordLinks`, default `[]`) and `describeLocalHousehold` content check | Foundation state | Merge of sibling roots; `tests/support/legacyShapes.mjs` V4_ROOTS | Integration | Wave 3 integration | NO | No schema version bump; persistence code untouched. |
| HK-INT-F12-STACK-01 | `supabase/tests/private-stack.mjs` accepts `HERKEYS_PRIVATE_DB` / `HERKEYS_PRIVATE_REST_NAME` (defaults unchanged) and applies the F12 migration | Test infrastructure | Keep; lets parallel sessions run private stacks without colliding | — | Wave 3 integration | NO | F12's journey uses `f12_stack` / `f12_postgrest` / ports 54397-54398. |
